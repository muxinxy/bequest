# 更新日志

## v0.6.4 (2026-08-15)

### 新增
- **FTP/SFTP 同步**：同步设置页新增 FTP/SFTP 协议(仅桌面/移动端,Web 端隐藏)——备份/恢复/自动备份全链路支持;基于 ftpconnect 与 dartssh2

### 修复
- **S3 下载 403**：GET 不再带 Content-Type,避免 web 端 CORS preflight 被跨域签名地址拒绝
- **S3 列表 403**：SigV4 canonical query 独立一行(原拼进 URI 导致验签失败)
- **保存 S3 后 WebDAV 配置丢失**：保存时与已存配置合并,两协议字段互不覆盖

### 优化
- 备份文件名用本地账户名称(如 `bequest_张三_...`);S3 下载/列表走平台请求(302 跟随 + no-referrer)

## v0.6.4 (2026-08-14)

### 同步与备份
- **S3 对齐 WebDAV**：S3 `download` 改走平台下载(302 跟随 + web 端 no-referrer),兼容 S3 兼容网关(CDN 重定向/防盗链)
- **修复:S3 下载 403(CORS preflight)**：`_signedHeaders` 原固定带 `Content-Type: application/json`——GET 下载无 body 却带此头,web 端 fetch 视为非简单请求 → 强制 CORS preflight → 跨域签名地址(OSS)preflight 失败 403。改为 upload 才带 Content-Type,GET/DELETE 不带(简单请求无需 preflight)
- **修复:S3 列表 403(SigV4 签名 bug)**：`listFiles` 带 query string(`?list-type=2&prefix=`),签名代码原把 query 拼进 canonical URI——SigV4 规范中 query 是独立一行,服务端验签不匹配 → 403。`s3AuthorizationHeader` 新增 `canonicalQuery` 参数,query 按键排序 + 键值各自编码
- **备份文件名用本地账户名称**：本地模式备份文件名取当前激活账户的名称(如 `bequest_张三_<设备名>_<时间戳>.json`),不再一律回退 'local';云端仍用用户名。auto_backup 同步复用
- **修复:保存 S3 后 WebDAV 配置丢失**：`_formConfig` 只返回当前协议字段,保存时另一协议配置被覆盖。`_save` 改为与已存配置合并(当前协议以表单为准,另一协议沿用已存值)

### 验证
- Flutter 152 测试全过(新增:S3 302 下载、带 query 签名、download 无 Content-Type、currentAccountName 本地账户名、配置合并、签名向量不回归)

## v0.6.3 (2026-08-14)

### 同步与备份
- **备份文件名自动生成**：不再手动输入——`bequest_<用户名>_<设备名>_<时间戳>.json`(用户名从 /me 获取,设备名取 Platform.localHostname,特殊字符安全清洗)
- **恢复弹窗显示备份文件列表**：文件名/修改时间/大小,支持恢复与删除;删除后自动刷新列表;列表不可用(服务器不支持 PROPFIND/List)时回退手动输入文件名(预填最近备份名)
- **自动备份**：同步设置页新增「自动备份」区——间隔(关闭/1/5/15/30 分钟/1/2/6/12/24 小时/打开应用时/退出应用时)+ 最大备份数量(1/3/5/10/20/50);后台调度器定时触发,开应用/退应用事件触发;备份失败静默记日志不打扰
- **备份轮转**：超过最大备份数量自动删除最旧备份(手动同步与自动备份均执行)
- **WebDAV 支持列文件/删除**：PROPFIND Depth:1 列目录(大小/修改时间),DELETE 删文件;兼容命名空间前缀大小写(OpenList 返回 `<D:>`、Nextcloud 用 `<d:>`),href URL 解码
- **S3 支持列文件/删除**：ListObjectsV2 + DeleteObject(手写 SigV4 签名)
- **修复:同步配置按本地账户隔离**：此前所有本地账户共享同一份 WebDAV/S3 配置;现按账户独立(云端模式仍用全局键)
- **修复:测试连接超时**：原串行 3 请求(MKCOL+PUT+DELETE)最坏 20s;改为单请求 PUT probe 5s 内必达;401/403 明确报认证失败

### 兼容性修复(阿里云盘 WebDAV 网关)
- **修复:备份列表恒为空**：PROPFIND 解析正则只认小写 `d:` 前缀,OpenList 返回大写 `<D:>` → 解析 0 文件 → "没有找到备份文件"
- **修复:恢复失败(302)**：网关文件 GET 返回 302 到 OSS 签名地址,http 包默认不跟随/跟随后带 Referer 被 OSS 防盗链 403 拒绝。改用平台分实现——Web 端原生 fetch + `referrerPolicy: 'no-referrer'` + redirect follow(跨域自动剥离 Authorization、无 Referer → OSS 200);桌面端手动跟随
- **修复:web 启动白屏**：自动备份调度器在 `runApp` 前访问 `WidgetsBinding.instance` 空指针;补 `WidgetsFlutterBinding.ensureInitialized()`
- **MKCOL 405 属预期**：目录已存在时服务器返回 405,代码视为"已存在"放行,无影响

### 验证
- Flutter 146 测试全过(新增:PROPFIND 大写 D: 解析、WebDAV/S3 list+delete、302 重定向下载、备份文件名生成、自动备份配置解析、同步配置账户隔离);web 构建成功

## v0.6.2 (2026-08-13)

### 本地模式
- **修复:本地模式账户列表无法返回登录页**：入口页曾禁用返回箭头与系统返回(视为退出 APP),用户只是进来看看也会被困。现入口(列表/新建/验证)允许返回登录页;进入本地主页后仍走「退出本地模式」按钮
- **修复:本地模式未分类资产无法移入其他分组**：批量移动/删除分组时把分组 id 经 `int.tryParse` 转换——云端 id 是 int64 没问题,本地 id 是 `'L<时间戳><序号>'` 字符串 → 转成 null → 资产被误移到「未分类」。`AssetRepository.moveAssets`/`deleteCategory` 接口改为传字符串分组 id,格式转换收敛到云端实现内部
- **修复:退出本地模式后应用锁仍有效**：退出时未清除应用锁凭据,回到登录页仍被锁屏拦截。现退出本地模式(`_exitLocal` 与锁屏跳过)一律 `clearAppLock()`(清 PIN/图案/开关),本地账户数据保留

### 主密码提示语
- **修复:本地模式锁屏/验证无主密码提示语**：账户提示语只存账户槽,标准槽为空——锁屏弹窗读标准槽读不到。修复:创建/激活本地账户时同步提示语到标准槽,退出本地模式恢复云端提示语;锁屏读取时标准槽为空则回退当前激活账户
- **锁屏主界面直接显示提示语**：不再只依赖点「用主密码解锁」后的弹窗——锁屏一出现即在「请输入主密码解锁」下方展示提示语

### 应用锁
- **设置页新增「启用应用锁」开关**：此前只能通过清空全部解锁方式间接关闭。开关关闭 → 保存时清除全部解锁凭据并禁用;开启 → 原配置项(锁定时机/生物识别/PIN/图案)生效
- **锁屏新增「跳过」按钮**：忘记解锁方式不再锁死——点击即退出当前模式回登录页(云端退出登录保留本机密钥;本地退出本地模式保留本地账户)。配套架构修复:锁屏位于独立 Navigator 覆盖层,无法操作主 Navigator,新增全局 `appNavigatorKey` + `LockGate.exitToLogin()` 完成清栈回登录页

### 验证
- Flutter 130 测试全过(新增:本地提示语兜底、锁屏主界面提示语、跳过按钮、moveAssets 字符串 id、deleteCategory moveTo、hint 槽位同步/恢复、clearAppLock)

## v0.6.1 (2026-08-13)

### 跨设备与主密码
- **修复:同设备每次登录都弹「恢复加密密钥」**：退出登录曾把主密钥/盐/WK 一并删除,再次登录被当成"新设备"。现加密凭据与服务器地址同属设备级状态,退出登录默认保留;恢复对话框仅在本机确无主密钥或本机盐≠服务端盐(主密码已在其他设备修改/重置)时弹出
- **退出登录可选清除密钥**：主页「退出登录」弹确认框——「保留密钥」(默认,下次登录免恢复)或「清除密钥」(公共电脑/彻底退出,下次登录重新恢复);锁屏退出出口本就是"无主密钥"分支,不受影响
- **修复:多端主密码不一致时资产静默解密失败**：云端资产解密失败(最常见原因:主密码已在其他设备修改)现弹「退出登录重新登录」对话框,重新登录走恢复流程重派生密钥;本地模式保留原提示(与多端无关)
- **修复:修改服务器地址后验证码仍请求旧地址**：登录页 ApiClient 改为按当前配置即时构造(不再缓存),从服务器设置页返回自动刷新验证码

### 邮件与 SMTP
- **修复:后台配置 SMTP 发不出邮件**——两处根因:
  - `net/smtp.SendMail` 只支持 STARTTLS(587),不支持 QQ/163 授权码默认的 **465 隐式 TLS**;重写 `sendViaServer` 手动拨号(465 → `tls.Dial`,587 → STARTTLS)
  - `buildMessage` 把 From/To 头整体 RFC2047 编码(无裸邮箱)→ **QQ 550 "From header missing or invalid"**;改为仅编码显示名、保留可解析邮箱(`From: =?UTF-8?B?名?= <noreply@qq.com>`),信封 `MAIL FROM` 剥离显示名
- **修复:用户自配 SMTP 失效时系统 SMTP 不兜底**：`sendMailCustom` 曾吞掉发送错误、`sendCustomForUser` 无条件返回成功——只要账号配过自 SMTP(哪怕已失效)系统 SMTP 就没机会发送。现仅真正发送成功才优先,失败回退系统 SMTP(重置验证码/提醒邮件同策略)
- **忘记密码验证码优先用户 SMTP**：`reset-request` 与提醒邮件一致(用户自配 → 系统回退);未知邮箱仍返回 200 不发码(防枚举,刻意保留)

### 防重复点击
- **发送验证码按钮 60s 冷却倒计时**：发送成功后按钮禁用并显示「重新发送(57 s)」,防连点刷接口
- **SMTP 设置保存/清除、同步页(测试连接/立即同步/从备份恢复/保存配置)补防连点守卫**

### 修复
- **恢复加密密钥弹窗被键盘遮挡**：对话框随 `viewInsets` 上移 + 内容可滚动,「忘记主密码?去重置」不再遮住账户密码输入框
- **保存服务器地址无反馈**：先验证连接、通过才保存并提示「已保存」,失败不保存并提示检查地址

## v0.6.0 (2026-08-12)

### 安全
- **继承领取限流 + 失败审计**：`/inheritance/claim` 与 `/auth/2fa/verify` 纳入严格限流窗口(5次/分/IP)；claim 失败(未知 event_key / 错误访问码)记审计日志
- **管理后台登录审计 + 2FA (TOTP)**：管理员完整登录(含 2FA 通过后)记 `admin_login` 审计(含来源 IP)；`/admin` 配置页「两步验证」——启用(生成密钥+动态码确认)/停用；登录两步走：账号密码 → `totp_required` + 5 分钟待验证令牌 → 动态码换正式令牌；待验证令牌被 requireAuth 拒绝。纯标准库实现(RFC 6238, HMAC-SHA1, ±1 步容差)
- **改密后旧 token 立即失效**：`users.token_version`(迁移 013)签名入 JWT,requireAuth/requireAdmin 校验——账户密码修改/重置后所有已签发 token 即刻作废,无需等 24h TTL
- **忘记密码加固**：`reset-request`/`reset` 纳入严格限流窗口;验证码 5 次错误自动作废(防多 IP 穷举 6 位码)

### 密码与主密码
- **修复:修改主密码后云端资产无法解密**：此前只更新 `master_key_wrapped`,未逐资产用新主密钥重包 `asset_key_wrapped_mk`——改密后云端资产全部解不开。修改流程现逐资产重包(凭据密文与继承包装原样保留,非破坏性)
- **修复:修改/重置主密码后,新设备恢复误报"主密码错误"**：两流程生成新盐但未同步服务端 → 新设备用旧盐派生 → 密钥不匹配。补 `updateMasterSalt` 同步新盐
- **登录后修改账户密码**：账号信息页新增「修改登录密码」(当前密码校验);配合 token_version 使全部设备退出登录
- **跨设备密钥恢复(ADR-17)**：注册上传 `master_salt`(明文不敏感),登录返回;新设备登录且本机无主密钥时弹「恢复加密密钥」对话框——主密码+盐重新派生 MK(非破坏性,凭据全保留)、WK 缺失则生成新 WK 并重包装;老账号由本机有盐设备登录自动回填(`PUT /api/v1/settings/master-salt`)
- **忘记主密码不死循环**：恢复对话框新增「忘记主密码?去重置」出口(重置只需账户密码,不需旧主密码),完成后直接进入主页
- **重置主密码 UX**：页面顶部引导"还记得主密码 → 用修改(数据不丢失)";重置确认框显示将影响的资产数

### 继承
- **继承人 Web 领取页**：`/claim` 单页——输入 event_key + 访问码即领取,展示交接密钥与反悔截止时间
- **72h 反悔倒计时**：领取时落 `reversable_until`(claimed+72h);窗口内号主登录可撤销,超期后交接最终完成;状态接口/继承状态页显示反悔截止
- **「继承人绑定资产」分组实体化**：列表返回分组/资产两类绑定条目——分组一行(含经分组继承资产数 `asset_count`,排除已资产级绑定),空分组也显示、可解绑;修复解绑分组绑定必失败(缺 `category_id` → 空 URL);分组行新增资产预览按钮(`GET /categories/{id}/inheritors/{iid}/assets`)

### 分组
- **删除分组保护**：确认框显示受影响资产数 + 可选「资产移入」目标分组(`DELETE /categories/{id}?move_to=`,先迁移再删,防误删散落未分类);即「合并分组」
- **分组排序**：`categories.sort_order`(迁移 012)+ 分类管理页上移/下移(`PUT /categories/order`);本地模式同样生效
- **分组头信息**：主页分组标题显示 30 天内到期预警图标
- **未分类批量整理**：主页「未分类」分组头移动图标,一键全部移至目标分组(`POST /assets/move`)
- 分类列表响应新增 `asset_count`/`sort_order`

### 本地模式
- **多本地账户**：可创建多个账户(各自独立主密码、加密数据与 vault 文件 `vault_<账户id>.bq`);进入先选账户、验证该账户主密码;可删除账户;旧版单账户自动迁移(沿用 vault.bq,零数据迁移)
- **云端/本地密钥隔离**：进入本地账户前暂存云端密钥槽、退出时恢复——互不覆盖(此前本地设置会覆盖云端主密钥槽)
- **本地分组排序/批量移动修复**：直接操作本机 vault,不再报"排序保存失败"

### 管理后台增强
- **用户资产明细**：用户列表「资产」按钮查看该用户资产元数据清单(密文服务端不可见)
- **审计日志导出 CSV**(UTF-8 BOM,Excel 直开)+ SMS/电话服务商配置页(发送仍预留)
- **管理后台时区显示**：审计日志/用户时间按浏览器本地时区显示(原为 UTC 原样,差 8 小时)

### 运维
- **/healthz 查 DB**：探活实际检测 SQLite 可达性(容器探活不再形同虚设)
- **备份命令**：`bequest-server backup` → `VACUUM INTO` 一致性快照;`PORT` 环境变量可换监听端口

### 修复
- **Web 端锁定按钮一闪而过**：已登录即不放行(锁屏提供主密码解锁 + 退出登录出口,防无凭据锁死)
- **验证码加载失败无限转圈**：显示"加载失败,点此重试"
- **本地模式返回出口**：入口去返回箭头,进入清登录页栈底,系统返回直接退出 APP;刷新不丢会话
- **Web 注册/本地模式设置主密码失败**：web 端 argon2 派生 `js_util.callMethod` 修复
- **config.json `from_addr` 失效**：`smtpServer` 补 json tag
- 免费资产上限错误信息硬编码 50 → 实际配置值

## v0.5.0 (2026-08-12)

### 新增
- **管理后台（ADR-16）**：浏览器访问 `/admin` 即可（单页内嵌，Go 同源托管，零新依赖、无需构建）
  - **仪表盘**：用户/会员/管理员/资产/分类/继承人/待领取继承事件/未读提醒 统计卡片
  - **用户管理**：搜索（用户名/邮箱）、会员开通/取消、管理员任命/撤销、禁用/启用、删除（级联删资产）；操作有自保护（不能降级/禁用/删除自己）
  - **系统配置**：SMTP 服务器轮询列表编辑 + 免费资产上限，保存即写入 `config.json` 热重载（密码不回显，留空保留原值）
  - **审计日志**：全量查看 + 按 user_id 筛选，管理员操作（actor='admin'）同样落审计
- **首个管理员引导**：设置 `ADMIN_USERNAME`/`ADMIN_PASSWORD` 环境变量，启动时自动创建（或提升）管理员；Docker compose 已预留
- **账号禁用**：`users.disabled`——禁用后该账号无法登录、存量 token 立即失效（403）
- **登录/me 返回 role**：客户端可识别管理员身份

### 修复
- **config.json `from_addr` 失效**：`smtpServer` 结构体缺 json tag，Go 大小写折叠匹配跨不过下划线，`from_addr` 字段一直被静默丢弃；已补 tag（管理后台配置保存依赖此修复）
- **免费资产上限错误信息硬编码 50**：改用实际配置值显示
- **Web 注册/本地模式设置主密码失败**：web 端 argon2 派生的 JS 绑定用 `js_util.callMethod(argon2id, 'call', [options])`，编译后变成 `argon2id.call(options)`——JS 的 `.call()` 把 options 当 thisArg 而非参数，hash-wasm 收到空参数报 `Invalid options parameter`；注册与本地模式设置主密码都调 `deriveMasterKey` 所以一起失败。已改 `callMethod(hashwasm, 'argon2id', [options])`，Node 加载编译产物验证派生结果与锚值逐字节一致

### 新增
- **注册/登录验证码**：算术验证码（如 `7 + 7 = ?`），服务端生成、答案 sha256 哈希存内存缓存（5 分钟过期、一次性防重放）；登录/注册校验 `captcha_id + captcha`，前端算式卡片可点击刷新，验证码错误自动刷新重试
- **按 IP 频率限制**：内存滑动窗口中间件——登录/注册 5 次/分钟/IP，其他 API 120 次/分钟/IP；超限 429，窗口自动恢复；支持 `X-Forwarded-For`
- **账号管理**：设置页可修改用户名/邮箱（`PUT /me`，冲突 409 提示）；登录支持用户名**或**邮箱；注册页用户名/邮箱边输边实时查重（`GET /auth/check`、`/auth/check-email`，防枚举）
- **忘记密码**：邮箱验证码重置（6 位数字，10 分钟有效，哈希存储防泄露；`POST /auth/reset-request` + `/auth/reset`）
- **注册表单增强**：登录密码与主密码均需输入两次；主密码提示语字段；主密码不能与登录密码相同；格式（用户名 3-20 位字母数字下划线、邮箱格式）边输边即时提示
- **登录/注册服务器预检**：点击后先 `GET /healthz`（2 秒超时），服务器不可达立即提示，不再转圈
- **服务器地址记忆**：最近使用地址（最多 5 条，chip 快捷填入、可单独清除）；去掉"测试连接"按钮，进入页面与保存时自动测试，地址框右侧绿/红指示灯

## v0.4.1 (2026-08-11)

### 修复
- **Android release 包无网络权限**：main AndroidManifest 缺 `INTERNET` 权限(仅 debug/profile 有)+ Android 9+ 默认禁明文 HTTP——手机安装 release 包后无法连接自托管服务器(浏览器正常是因为浏览器自带权限)。已加 `INTERNET` + `usesCleartextTraffic="true"`
- **Web 端锁定按钮无效**：锁屏把"仅设主密码"误判为"无解锁方式"自动放行。自动放行条件补 `!_hasMasterKey`,手动锁定改为无条件触发(锁屏自身处理无凭据场景)

### 新增
- **账号密码重置主密码**：忘记主密码时用账户密码重置(验证账户密码→新主密钥→云端资产换新密钥重加密);端到端加密的固有代价——旧凭据不可恢复,重置后资产保留名称/分类,凭据需重新填写
- **分组视图**：主页资产按分组(分类)分组展示,分组标题可折叠展开
- **搜索分组与资产**：搜索同时匹配分组名和资产名
- **分组继承人**：分组可绑定一个或多个继承人(继承触发时该分组下未单独绑定的资产按分组继承人交接,可设独立触发天数)
- **继承开关**：设置页一键开启/关闭继承功能(关闭后调度器跳过升级提醒与继承触发)
- **继承人绑定资产**：设置页查看某继承人绑定的所有资产(直接绑定 + 经分组继承),支持多选解绑

## v0.4.0 (2026-08-11)

### 新增
- **Web 客户端**：同一套 Flutter 代码编译 Web，由服务端同源托管（`go run .` 即自动服务 `app/build/web`，浏览器打开 `http://localhost:8080`）；web 端默认相对路径免 CORS，服务端补 CORS 中间件支持独立部署
- **资产级密钥隔离（继承升级）**：每个资产独立加密密钥（AK），内容用 AK 加密，AK 分别被主密钥（号主）与继承包装密钥（继承人）包装上传；资产可单独绑定一个或多个继承人并设独立触发天数；继承触发时按资产生成独立事件，继承人领取**只拿到指定资产的密钥**，不再持有整个保险箱
- **手动锁定**：主页新增锁定按钮，点击立即锁定应用（无需等待退出/超时）
- **加密导出**：导出资产可选「用主密码加密」（`.beq` 文件），导入时自动检测解密
- **覆盖导入**：导入资产可选「覆盖现有数据」（先清空再导入，带确认框）
- **客户端时区显示**：审计日志、继承状态、提醒列表时间统一按客户端时区格式化（服务端为 UTC 无时区标记，`formatServerTime` 正确处理）
- **提醒模板变量提示**：模板编辑弹窗列出可用变量 `{name}` 资产名 / `{date}` 到期日 / `{days}` 未登录天数

### 改进
- **Web 端派生性能**：argon2 主密码派生改走自托管 hash-wasm WASM（单文件、无 CDN 依赖），派生耗时从 ~33 秒降至 ~0.3 秒（快 113 倍）；注册/导出/导入不再卡死。移动端保持 pointycastle，派生结果与旧实现逐字节一致（锚值测试锁定，现有用户不受影响）

## v0.3.5 (2026-08-11)

### 修复
- **生物识别设置语义不准确**：移除无法由系统强制执行的指纹/人脸二选一，改为统一的「生物识别解锁」开关，由设备选择实际验证方式
- **旧版生物识别开关无法关闭**：迁移旧布尔配置后清理旧键，避免关闭后被旧配置重新开启
- **Docker 迁移文件缺失**：迁移 SQL 内嵌到后端二进制，修复容器启动时找不到 `migrations/` 目录的问题

### 文档
- 更新部署、开发进度和架构文档，明确 Docker 运行时只需持久化 `/data`

## v0.3.4 (2026-08-10)

### 修复
- **锁屏「用主密码解锁」崩溃**：AppLockScreen 此前无 Navigator 宿主，弹主密码输入框时抛 no-Navigator 异常；现已自建 Navigator，解锁流程抽为共享纯逻辑（`masterPasswordUnlock`）并补回归测试
- **生物识别设置显示不实**：设置页按设备真实能力展示指纹/人脸（不再一律显示可用）
- **Docker 数据目录权限**：镜像预创建 `/data` 并归属 appuser，compose 改用命名卷（继承权限），解决容器内无写权限导致的 Permission denied；bind mount 场景的 chown 步骤已写入文档
- **Docker 迁移文件缺失**：迁移 SQL 改为嵌入服务器二进制，修复容器启动时找不到 `migrations/` 目录的问题

## v0.3.3 (2026-08-09)

### 修复
- **生物识别解锁失效**：MainActivity 改为 FlutterFragmentActivity（local_auth 要求宿主为 FragmentActivity），系统指纹/人脸弹窗现在能正常出现
- **图案锁限流失效**：短图案（<4 点）尝试此前不计数，现统一计入失败（5 次 → 锁定 60 秒），与 PIN 对齐

### 改进
- **指纹/人脸二选一**：应用锁生物识别设置为 指纹/人脸/关闭 三选，检测设备支持情况并提示；未录入生物识别时解锁给出明确指引
- **用主密码解锁应用锁**：锁屏新增「用主密码解锁」（主密码为最强凭据，自带限流）
- **修改主密码与提示语**：设置新增「修改主密码」——验证旧密码后重加密本地库、更新提示语，登录时同步云端继承密钥（账户密码验证）

## v0.3.2 (2026-08-09)

### 改进
- **移除「实体/虚拟」类型**：资产表单、列表筛选、分类管理的类型选择全部移除，资产分类更简洁（后端字段兼容保留）
- **锁设置改名「应用锁」**：入口与界面统一命名
- **生物识别失败提示具体原因**：未录入指纹/人脸、设备不支持、系统暂时/永久锁定等分别给出中文提示（用户主动取消不提示）
- **防暴力尝试**：应用锁（PIN/图案）与主密码连续失败 5 次 → 锁定 60 秒（倒计时提示并禁用输入），成功解锁清零；生物识别失败不计入
- **主密码提示语**：设置主密码时可填写提示语，解锁/导入导出/恢复时显示帮助回忆
- **日志增强**：按天分文件 + 总量 1MB 自动淘汰最旧；记录全部 API 请求（方法/路径/状态/耗时）与关键操作，便于排查问题

### 修复
- 新增资产后点击报「资产不存在或本地数据异常」：主页筛选链路的 `toJson→fromJson` 往返丢失资产 id，现已修复并加回归测试

## v0.3.1 (2026-08-09)

### 修复
- **正式签名**：Release 使用独立 keystore（GitHub Secrets 配置），APK 版本间可直接覆盖安装，不再提示签名不一致
- **产物命名**：APK 与二进制带产品名和版本号（`bequest-v0.3.1-arm64-v8a.apk` / `bequest-server-v0.3.1-linux-amd64`）
- **本地模式**：点开资产不再误报"请检查网络后重试"，改为准确的本地数据提示
- **图案锁**：加大命中范围 + 线段穿点检测，快速拖拽也能顺畅连点
- **应用锁**：本地模式下同样生效（此前无账号时不锁定），退出重进 / 超时均正确触发

### 新增
- **关于本应用**：版本、包名、简介、GitHub 链接（可打开/复制）
- **日志收集**：关键错误自动记录到本地文件，设置中可导出日志便于反馈调试

## v0.3.0 (2026-08-09)

### 改进
- **分类全面升级**：分类类型化（实体/虚拟，新增时可选）；预设分类改为服务端数据，与自定义分类一致支持改名、改类型、删除；分类编辑/删除入口补齐
- **资产删除**：编辑页新增删除（确认后不可恢复）
- **列表增强**：实体/虚拟筛选 + 分类筛选 + 名称搜索（不区分大小写）
- **设置收纳**：主页右上角精简为 提醒/设置/退出；设置页分组收纳 9 个入口（分类、导入导出、模板、继承人、继承状态、锁、SMTP、审计、存储/服务器）
- **锁设置重做**：可选择「退出时锁定」或「退出且超时锁定」（1-60 分钟）；解锁方式支持 PIN / **图案（3×3）** / 系统指纹或人脸；解锁后进入本地模式**不再重复输入主密码**
- **发布更新说明**：Release 自动携带 CHANGELOG 中文说明（人工维护，按版本提取）

### 修复
- 修复锁设置不生效、分类无法删除/改名、新增分类无法选择类型等反馈问题

## v0.2.0 (2026-08-09)

### 新增
- **云/本地双模存储**：不登录也能完整使用——「进入本地模式」设置主密码后，资产/分类全量本地加密管理（`AssetRepository` 云/本地双实现，可随意切换，切换时自动迁移数据）
- **跨设备恢复**：同步负载携带密钥盐，新设备输入「主密码 + 备份文件」即可全量恢复；本地 JSON 导入同样支持
- **三层权益**：访客（本地 20 条）/ 免费用户（云端 50 条 + 继承）/ 会员（不限）——首页徽章 + 创建超限拦截
- **服务器地址可配置**：设置页自定义后端地址（自托管用户可指向自己的服务器）
- **Android 安装包**：Release 提供 arm64-v8a / armeabi-v7a / x86_64 三个 APK

### 改进
- 分类类型化（实体/虚拟）、可编辑/可删除；预设分类改为服务端数据，与自定义分类一致管理
- 资产支持删除；列表支持实体/虚拟筛选与名称搜索
- 设置收纳到二级页面（AppBar 只留 提醒/设置/退出）
- 锁设置增强：退出时锁定 / 退出且超时锁定、PIN / 图案 / 指纹 / 人脸解锁、解锁后免主密码
- 自托管同步无需登录；自定义 SMTP 发件；多 SMTP 负载均衡；审计日志页

### 修复
- 替换与 Flutter 3.44 不兼容的 file_picker（KGP 冲突导致 APK 构建失败）→ 官方 file_selector

## v0.1.0 (2026-08-09)

### 新增
- 数字资产保险箱 + 数字遗嘱：实体/虚拟资产管理，端到端加密（AES-256-GCM，主密码派生）
- **继承交接（dead man's switch）**：不登录升级提醒（免费 30/60/90/120 天、会员 7/14/30/60 天）→ 触发交接 → 继承人凭「事件密钥 + 预设访问码」双因子领取解密密钥；三重取消窗口（触发前登录 / 领取前 / 领取后登录反转）
- 提醒系统：资产到期提醒（30/7/1 天 + 已到期）、站内信 + 邮件（SMTP）、默认/自定义模板
- 自托管同步：WebDAV / S3 加密备份（配置仅存本机，凭据不经服务端）
- 导入导出：JSON（全部/分类/单条，主密码验证）
- 安全：APP 锁（PIN + 生物识别）、免费/会员权益、审计日志
- 部署：Docker Compose、5 平台二进制、GitHub Actions 自动发布（含 Docker 镜像 ghcr.io/muxinxy/bequest）
